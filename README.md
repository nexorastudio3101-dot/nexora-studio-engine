# NEXORA Studio Engine — Web

Cloud-ready build of the NEXORA Studio Engine.

The service binds to the hosting platform's PORT and includes Docker configuration for FFmpeg and font dependencies.

Deploy as a Docker web service. No local Mac installation is required for the deployed version.
